# Bind address. In production this should stay 127.0.0.1 - Apache is meant
# to be the only thing the internet can reach; the app holds no public
# socket. install.py defaults to this and warns if you override it to
# 0.0.0.0. Only change this if you know Apache is NOT fronting this box.
APP_HOST=127.0.0.1
APP_PORT=8000
# development | production. production enforces the fail-fast checks below
# (refuses to boot on an unsafe SECRET_KEY or missing ALLOWED_HOSTS) and
# turns on COOKIE_SECURE/HSTS by default. Use development only on a machine
# nothing but you can reach.
APP_ENV=development

# First-boot bootstrap ONLY. On an empty database these seed exactly one
# admin account with a forced password change at first login. Every boot
# after that ignores both values completely - they never resurrect a
# deleted account or overwrite a changed password. Safe (and recommended)
# to delete both lines once you've logged in and changed the password.
API_USERNAME=admin
API_PASSWORD=admin
# Generate a real secret: python -c "import secrets; print(secrets.token_hex(32))"
# Required in production - the app refuses to boot if this is missing,
# under 32 characters, or still the placeholder below.
SECRET_KEY=change-me-in-production

# The public hostname(s) this server answers on, comma-separated (e.g.
# zk.example.com). Requests with any other Host header are rejected before
# they reach a route. Required in production - the app refuses to boot
# without this set, or set to "*".
ALLOWED_HOSTS=

# Addresses trusted to hand the app an accurate X-Forwarded-For - i.e.
# Apache's own source address. Almost always 127.0.0.1 (and ::1 if either
# side prefers IPv6 loopback) when Apache and the app run on the same host.
# Get this wrong and per-device IP allowlisting (Devices -> per-device CIDR
# list) either fails closed (every device refused) or fails open (spoofable
# via a forged header) - see deploy/apache/zkteco-sync.conf.example for the
# full explanation.
TRUSTED_PROXIES=127.0.0.1,::1

# Whether the session cookie carries the Secure flag (HTTPS-only). Defaults
# to true in production, false in development - a plain-HTTP dev run would
# otherwise never get the cookie back from the browser. Only set this
# explicitly to override that default.
COOKIE_SECURE=

# A session is revoked after this many minutes of inactivity...
SESSION_IDLE_MINUTES=60
# ...or this many hours since login, whichever comes first, even with
# continuous activity.
SESSION_ABSOLUTE_HOURS=12

# Failed logins allowed before an account is locked out...
LOGIN_MAX_ATTEMPTS=5
# ...for this many minutes.
LOGIN_LOCKOUT_MINUTES=15

# Expose the interactive API docs (/docs, /redoc, /openapi.json). Leave
# false on a public-internet deployment - they hand an attacker a complete,
# browsable map of every endpoint. Turn on only for local API exploration.
ENABLE_DOCS=false

# Largest request body accepted, in bytes, on any route (default ~2 MiB).
# Oversized requests get a 413 before the body is fully buffered.
MAX_REQUEST_BYTES=2097152

# --- ZKTime .NET backup: Backup and Restore (Settings) -------------------
#
# A ZKTime backup is a whole SQLite database, far past MAX_REQUEST_BYTES, so
# /backup/upload has its own ceiling. Everything else keeps the limit above.
# Raise this only if your own backup is bigger; the default holds several
# times the size of a typical multi-year, multi-terminal file (~128 MiB).
BACKUP_MAX_UPLOAD_BYTES=134217728

# Where an uploaded backup waits between the preview and the restore.
# Defaults to a subdirectory of the system temp dir, created mode 0700. Set
# this if /tmp is small, noexec-mounted or cleared aggressively.
#BACKUP_STAGE_DIR=/var/lib/zkteco-sync/backup-staging

# How long a staged upload survives before it is deleted unread, in seconds
# (default 1800 = 30 min). It holds staff records and fingerprint templates,
# so this is deliberately short; expired files are purged on the next upload.
BACKUP_STAGE_TTL_SECONDS=1800

# Where the export *template* is kept: the ZKTime backup a Backup is built on.
# Most of a ZKTime database is its own configuration and cannot be generated,
# so Backup builds on a real one. A successful Restore saves its own file here
# automatically, which is what makes Backup a single button.
#
# Unlike the staging directory above this is meant to persist. The default is
# <app>/data, which works on a native install. In Docker /app is root-owned
# and the app user cannot write there, so docker-compose.yml sets this to a
# mounted volume — do not remove that. Losing the file is recoverable: Backup
# asks for a template again.
#BACKUP_DATA_DIR=/var/lib/zkteco-sync
# Or name the file outright, overriding BACKUP_DATA_DIR:
#BACKUP_TEMPLATE_PATH=/var/lib/zkteco-sync/zktime_template.db

# How long a device-pairing window (Devices -> open pairing) stays open, in
# minutes, before it auto-closes and unknown serials go back to being
# refused instead of queued for approval.
ADMS_PAIRING_MINUTES=15

# Most rows one Attendance -> Export Excel may build into a workbook. The
# export always covers the whole filter, not the page on screen, so this is
# the guard that stops an unfiltered export of every punch ever recorded from
# exhausting memory. A request matching more rows than this is refused with a
# message naming the count, and the operator narrows the date range.
ATTENDANCE_EXPORT_MAX_ROWS=200000

# --- The shift the timesheet is scored against -------------------------------
# The monthly Excel timesheet does not just list punches: it reports hours
# worked, hours owed, lateness, early departures and absences, and every one
# of those is a punch measured against a shift. These are that shift. The
# defaults are the one the terminal's own ZKTime.Net software scored this
# install by.
#
# "Require Work" is not a setting - it is derived as the paid length of the
# shift (end - start - break), so the two can never disagree.
WORK_SHIFT_START=08:30
WORK_SHIFT_END=17:30
# The unpaid break. It comes off a day's hours only when somebody was present
# for the whole of it, and it is excluded from lateness and early departure -
# leave at 12:01 and you are 4:30 early, not 5:29.
WORK_BREAK_START=12:00
WORK_BREAK_END=13:00
# Which weekdays carry the shift, ISO numbers (Monday=1 ... Sunday=7). A day
# outside this set is a rest day: no timetable, nothing owed, nothing absent.
WORK_DAYS=1,2,3,4,5
# The name written in the timesheet's "Bang thoi gian" (timetable) column on a
# working day. Blank on a rest day.
WORK_TIMETABLE_NAME=Default

# What a newly registered device's clock is assumed to be set to. ZKTeco
# terminals push a bare wall-clock time with no offset and no zone name, so
# something has to say what those digits mean - this is that answer, and it
# is stamped onto each device and snapshotted onto every attendance record.
# Nothing is ever converted: the punch time is stored and pushed exactly as
# the device reported it, with this label attached. Must be an IANA name
# (Asia/Dubai, Europe/London, America/New_York); the app refuses to start on
# a name the tz database does not know. Correct it per device afterwards in
# Devices -> Timezone if a terminal sits in another zone.
DEFAULT_DEVICE_TIMEZONE=Asia/Dubai

# --- Docker only -----------------------------------------------------------
# Read by docker-compose.yml, never by the app. Which host interface the
# published port binds to: 127.0.0.1 keeps it reachable only by a reverse
# proxy on this machine; 0.0.0.0 exposes it on the LAN (devices push directly).
# For the bundled database set DB_HOST=db and DB_PORT=3306 below, and
# DB_USER must NOT be root. TRUSTED_PROXIES must be 172.30.0.1 (the compose
# network gateway - see DEPLOY.md) rather than 127.0.0.1 when Apache on the
# host proxies to the container. Ignored by a native install.
APP_BIND=127.0.0.1

# DB_ENGINE options: mariadb, mysql, postgresql, mssql
DB_ENGINE=mariadb
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=zkteco_sync
DB_USER=root
DB_PASSWORD=

# Only required when DB_ENGINE=mssql
# Common values:
#   ODBC Driver 17 for SQL Server
#   ODBC Driver 18 for SQL Server
#   SQL Server Native Client 11.0
DB_ODBC_DRIVER=ODBC Driver 17 for SQL Server

# MSSQL Windows Authentication: leave DB_USER and DB_PASSWORD both empty
# and the app will automatically use trusted_connection=yes (no credentials in URL)
